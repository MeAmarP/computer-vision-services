input_dir: "data_input/videos/"           # Directory for input images/videos
output_dir: "infer_output/"         # Directory to save annotated outputs
task: "keypoint_detection"            # image_classification|object_detection|semantic_segmentation|instance_segmentation|keypoint_detection
model_name: "keypointrcnn_resnet50_fpn"  # Keypoint detection model
model_weights_class: "KeypointRCNN_ResNet50_FPN_Weights"
model_weights: "COCO_V1"
threshold: 0.4               # Confidence threshold for predictions
